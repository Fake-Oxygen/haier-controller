description: "Haier heatpump"
compatible: "haier,heatpump"
include: [base.yaml]

properties:
  slave-addr:
    type: int
    required: true

  enable-gpios:
    type: phandle-array
    required: true
